<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reverb Studio</title>
    <style>
        * {
            box-sizing: border-box;
            padding: 0;
            margin: 0;
        }

        #container {
            width: 100%;
            height: 100vh;
            display: flex;
            /* background-color: red; */
            justify-content: center;
            align-items: center;
            background-color: black;
        }

        #container form {
            /* background-color: grey; */
            width: 500px;
            padding: 20px;
            /* box-shadow: 0 0 20px #ababab; */
            border: 6px solid #3d3d3d;
            border-radius: 8px;
            position: relative;
        }

        form input[type="text"],
        input[type="password"],
        textarea {
            width: 100%;
            border: none;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: 2px solid #3d3d3d;
            background-color: #3d3d3d;
            color: #ababab;
        }

        form input[type="submit"] {
            border: none;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            position: relative;
            left: 50%;
            transform: translateX(-50%);
            border: 2px solid #3d3d3d;
            background-color: #3d3d3d;
            color: #ababab;
        }

        form input[type="submit"]:hover {
            cursor: pointer;
        }

        form .heading {
            text-align: center;
            margin: 10px 0;
            color: #ababab;
        }

        .lobo {
            width: 200px;
            margin: 50px;
        }

        .res-message {
            color: #ababab;
            text-align: center;
            margin: 10px 0;
        }
    </style>
</head>

<body>
    <div id="container">

        <img src="./logo.svg" class="lobo" alt="" srcset="">

        <form action="/add-song" method="post" autocomplete="off">
            <h3 class="heading">Add Song into the library</h3>
            <input id="songSearchQuery" type="text" name="songSearchQuery" , placeholder="Enter song search query from Instagram">
            <br>
            <input id="password" type="password" name="password" placeholder="Enter Password">
            <br>
            <input type="submit" value="Submit">

            <p class="res-message"><%= message %></p>
        </form>
    </div>
</body>

</html>